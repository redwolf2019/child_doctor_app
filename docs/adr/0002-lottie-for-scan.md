# 扫描过程用 Lottie，界面用 Flutter

点「开始扫描」之后的扫描线、肠道和虫子用 Lottie 播放。开始扫描、洗手提示文字、「再看一次」和配音控制用 Flutter。

不用整段视频，是因为手机和平板都要横屏适配，视频分辨率和包体都会被最大屏幕拖着走。不用 Rive，是因为扫描点一下就自动播完，不需要状态机交互。不用纯 Flutter 绘图，是因为卡通质感主要靠现成动画文件，而不是代码补间。

若 Lottie 做不出扫描质感，或文件大会拖慢低端机，再评估改成视频或拆成多段 Lottie。
